export const getAllTasks = async (
  req: Request,
  res: Response
): Promise<void> => {
  try {
    // const tasks: Task[] = await Task.find();
    // res.status(200).json({ tasks });
  } catch (error) {
    throw error;
  }
};
